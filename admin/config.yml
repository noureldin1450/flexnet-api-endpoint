backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)

# These lines should *not* be indented
media_folder: "static/images/uploads" # Media files will be stored in the repo under static/images/uploads
public_folder: "images/uploads" # The src attribute for uploaded media will begin with /images/uploads

collections:
  - name: "content" # Used in routes, e.g., /admin/collections/blog
    label: "movies" # Used in the UI
    folder: "content/movies" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string", default: "MovieName"}
      - {label: "slug", name: "slug", widget: "string", default: "MovieName Year"}
      - {label: "Publish Date", name: "date", widget: "string"}
      - {label: "moviecover", name: "moviecover", widget: "string"}
      - {label: "trailer", name: "trailer", widget: "string"}
      - {label: "year", name: "year", widget: "string", default: "Year"}
      - {label: "duration", name: "duration", widget: "string", default: "Ex:1h 50min"}
      - {label: "rating", name: "rating", widget: "string", default: "Ex:9.1"}
      - {label: "tags", name: "tags", widget: "list", default: "ex, ex, ex"}
      - {label: "stars", name: "stars", widget: "list", default: "StarName,StarName, StarName "}
      - {label: "story", name: "story", widget: "string", default: "Movie Story"}
      - {label: "imdbId", name: "imdbId", widget: "string", default: "Ex: tt000000"}
      #- {label: "Body", name: "body", widget: "markdown"}