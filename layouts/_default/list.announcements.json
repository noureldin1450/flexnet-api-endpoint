{
    "announcement":{
        {{ range $.Site.Data.announcement }}
            "title": "{{.title}}",
            "dis": "{{.dis}}",
            "enabled": {{.enabled}}
        {{end}}
    }
        
}
